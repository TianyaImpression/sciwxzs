% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{call_ai_api}
\alias{call_ai_api}
\title{统一 AI API 调用函数}
\usage{
call_ai_api(
  messages,
  api_key,
  provider = "deepseek",
  custom_url = NULL,
  model = NULL,
  max_tokens = 8000,
  temperature = 0.3,
  timeout_sec = 30,
  response_format = NULL
)
}
\arguments{
\item{messages}{消息列表，每项包含 role 和 content}

\item{api_key}{API 密钥}

\item{provider}{API 提供商类型，"deepseek" 或 "custom"}

\item{custom_url}{自定义 API 端点 URL（仅 provider = "custom" 时有效）}

\item{model}{模型名称，若为 NULL 则使用默认模型}

\item{max_tokens}{最大 Token 数}

\item{temperature}{温度参数 (0-2)}

\item{timeout_sec}{超时时间（秒）}

\item{response_format}{响应格式，如 list(type = "text") 或 list(type = "json_object")}
}
\value{
成功时返回 API 响应中的文本内容；失败时返回以 "失败：" 开头的错误信息
}
\description{
支持 DeepSeek V4 和用户自定义 OpenAI 兼容 API 的统一调用接口。
统一处理 API 端点、认证头、请求体构建和错误处理。
}
\examples{
\dontrun{
call_ai_api(
  messages = list(
    list(role = "user", content = "Hello")
  ),
  api_key = "sk-xxxx",
  provider = "deepseek",
  max_tokens = 50
)
}
}
